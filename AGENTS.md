# AGENTS.md

Build the MVP described in `docs/CODEX_TASK.md`.

## Requirements
- Use simulated demo records only.
- Use Chinese UI text.
- Keep configurable logic in JSON files.
- Add local startup and Docker startup instructions.
- Run backend tests and frontend build checks before finishing.
- Keep the implementation simple and suitable for a demo.
